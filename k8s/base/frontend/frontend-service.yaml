apiVersion: v1
kind: Service
metadata:
  name: spree-frontend
  namespace: dmz
spec:
  type: NodePort
  selector:
    app: spree-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
